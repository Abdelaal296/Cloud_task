pipeline {
    agent any
    stages {
        stage('List Files') {
            steps {
                echo "Hellllllo"
                sh 'chmod +x test.sh'
                sh 'test.sh'
            }
        }
    }
}
